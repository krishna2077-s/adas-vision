# Video files (too large for git — share via releases or externally)
*.mp4
*.avi
*.mkv
*.mov

# Screenshots / generated output
screenshot_*.jpg
logs/

# Downloaded model weights (auto-downloads on first run)
*.pt

# Trained segmentation weights (Module 1c) — large; produced by the Colab
# notebook, shared via a GitHub Release rather than committed to the repo.
*.pth
*.onnx
*.onnx.data

# OpenVINO IR (Phase 8) — large model artifacts produced by export_openvino.py;
# share via a Release, not the repo. Plus the Intel GPU compiler's scratch dump.
*_ov_fp16.xml
*_ov_fp16.bin
*_ov_int8.xml
*_ov_int8.bin
kernel.errors.txt
*.blob

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments
.venv/
venv/

# OS
.DS_Store
Thumbs.db
desktop.ini
# Installers / binaries
*.exe
*.msi
